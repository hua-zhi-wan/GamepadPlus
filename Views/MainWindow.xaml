<Window x:Class="AnotherGamepadPlus.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnotherGamepadPlus"
        mc:Ignorable="d"
        Title="AGP" Height="400" Width="500"
        Closing="Window_Closing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 手柄连接状态 -->
        <Label Grid.Row="0" Grid.Column="0" Content="手柄状态:" Margin="5"/>
        <Label Grid.Row="0" Grid.Column="1" x:Name="ControllerStatusLabel" Content="未连接" Margin="5" Foreground="Red"/>

        <!-- 当前屏幕 -->
        <Label Grid.Row="1" Grid.Column="0" Content="当前屏幕:" Margin="5"/>
        <Label Grid.Row="1" Grid.Column="1" x:Name="CurrentScreenLabel" Content="未知" Margin="5"/>

        <!-- 鼠标位置 -->
        <Label Grid.Row="2" Grid.Column="0" Content="鼠标位置:" Margin="5"/>
        <Label Grid.Row="2" Grid.Column="1" x:Name="MousePositionLabel" Content="(0, 0)" Margin="5"/>

        <!-- 灵敏度设置 -->
        <Label Grid.Row="3" Grid.Column="0" Content="灵敏度:" Margin="5"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <Slider x:Name="SensitivitySlider" Minimum="1" Maximum="30" Value="10.0" Width="200" 
                    ValueChanged="SensitivitySlider_ValueChanged"/>
            <Label x:Name="SensitivityValueLabel" Content="10.0" Width="50" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- 死区设置 -->
        <Label Grid.Row="4" Grid.Column="0" Content="摇杆死区:" Margin="5"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <Slider x:Name="DeadZoneSlider" Minimum="0.0" Maximum="0.5" Value="0.1" Width="200"
                    ValueChanged="DeadZoneSlider_ValueChanged"/>
            <Label x:Name="DeadZoneValueLabel" Content="0.1" Width="50" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- 操作说明 -->
        <GroupBox Grid.Row="5" Grid.ColumnSpan="2" Header="操作说明" Margin="5">
            <TextBlock TextWrapping="Wrap" Margin="5">
                左摇杆: 控制鼠标移动<LineBreak />
                A键: 鼠标左键<LineBreak />
                B键: 鼠标右键<LineBreak />
                X键: 鼠标中键<LineBreak />
                按住左肩键(LB): 精确模式<LineBreak />
                按住右肩键(RB): 快速模式<LineBreak />
                左扳机键(LT): 鼠标滚轮上滚<LineBreak />
                右扳机键(RT): 鼠标滚轮下滚<LineBreak />
            </TextBlock>
        </GroupBox>
    </Grid>
</Window>
